machine:
  node:
    version: 6

general:
  branches:
    only:
      - master

test:
  override:
    - npm run validate
  post:
    - npm run report-coverage

deployment:
  release:
    commands:
      - npm run release